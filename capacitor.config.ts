import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sama deukouway',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
